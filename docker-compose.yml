version: '3'

services:
  ieeags_app:
    build: 
      context: .
    env_file: .env
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/prueba
    ports:
      - "8000:8000"
    

  